{
  "name": "@aurion/engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    },
    "./package.json": "./package.json"
  },
  "files": ["dist", "src"],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --coverage",
    "test:watch": "vitest",
    "sim": "tsx scripts/sim.ts",
    "sim:easy": "SIM_DIFFICULTY=easy tsx scripts/sim.ts",
    "sim:normal": "SIM_DIFFICULTY=normal tsx scripts/sim.ts",
    "sim:hard": "SIM_DIFFICULTY=hard tsx scripts/sim.ts",
    "sim:compare": "tsx scripts/sim-balance.ts",
    "lint": "echo '@aurion/engine: no eslint configured (pure TS, lints via tsc strict mode)'"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.0.5",
    "fast-check": "^3.23.2",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "vitest": "^3.0.5"
  }
}
